Scaffold a new module in `src/retirement/` with a corresponding test file in `tests/`.

The user will provide the module name as an argument: $ARGUMENTS

1. Create `src/retirement/<name>.py` with a minimal placeholder
2. Create `tests/test_<name>.py` with a placeholder test
3. Show the created files
